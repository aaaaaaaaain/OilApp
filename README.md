# 油耗紀錄 App ⛽

記錄每次加油的里程與公升數，自動算出油耗（km/L）。純前端網頁，資料存在瀏覽器裡，不上傳任何伺服器。

## 功能

- **兩種輸入模式**
  - 直接輸入里程：這趟開了幾公里 ÷ 加了幾公升
  - 總公里數計算：只要抄里程表讀數，App 自動跟上次相減
- **統計卡片**：平均油耗、總筆數、歷史新低
- **折線圖**：油耗變化趨勢（Chart.js）
- **歷史明細**：每筆顯示油耗、與上一筆的增減（↑↓）、間隔天數，可編輯／刪除
- **匯出 Excel**：含每筆明細與總計列（總公升、總里程、總平均）
- **載具條碼**：輸入 7 碼手機載具代碼，產生 CODE128 條碼，加油時直接給店員掃
- **深色模式**：跟隨系統設定
- **可加到手機主畫面**：iOS 加入主畫面後全螢幕開啟

## 使用方式

線上直接用（若已開啟 GitHub Pages）：

```
https://aaaaaaaaain.github.io/OilApp/
```

或本機開啟：

```bash
git clone https://github.com/aaaaaaaaain/OilApp.git
cd OilApp
python -m http.server 8123
```

然後瀏覽器打開 http://localhost:8123

> 建議用本機伺服器開啟，直接雙擊 `index.html` 在部分瀏覽器會擋掉外部函式庫。

## 計算方式

| 情況 | 行駛里程 | 油耗 |
| --- | --- | --- |
| 直接輸入里程 | 你填的里程 | 里程 ÷ 公升 |
| 總公里數，第一筆 | 里程表讀數（從 0 起算） | 讀數 ÷ 公升 |
| 總公里數，第二筆之後 | 這次讀數 − 上次讀數 | 里程 ÷ 公升 |

- 上方「平均 km/L」= 所有算得出油耗的紀錄取平均
- Excel 的「總平均」= 總里程 ÷ 對應的總公升數（加權平均，跟上方數字算法不同）
- 紀錄會依日期時間自動排序，插入舊日期的資料也會重新計算

> 若你的車是中古車、里程表已經有幾萬公里，第一筆會算出偏高的數字，把它當基準看第二筆之後即可。

## 檔案結構

```
index.html   版面與四個頁籤（新增／圖表／歷史油耗／載具）
style.css    iOS 風格樣式，含深色模式
script.js    全部計算、儲存、圖表、匯出、條碼邏輯
fuel.png     加到主畫面時的圖示
```

## 技術

- 原生 HTML / CSS / JavaScript，無框架、無建置流程
- [Chart.js](https://www.chartjs.org/) — 折線圖
- [SheetJS](https://sheetjs.com/) — Excel 匯出
- [JsBarcode](https://github.com/lindell/JsBarcode) — 載具條碼

## 資料儲存

所有紀錄存在瀏覽器的 `localStorage`：

| Key | 內容 |
| --- | --- |
| `fuelRecords` | 加油紀錄陣列 |
| `carrierCode` | 載具代碼 |
| `inputMode` | 上次用的輸入模式 |

因此：

- 換瀏覽器、換手機、清除瀏覽資料 → 紀錄會不見，記得定期用「導出 Excel」備份
- 「重置資料」會清空紀錄，無法復原
